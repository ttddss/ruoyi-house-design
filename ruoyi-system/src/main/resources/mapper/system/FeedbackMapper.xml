<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.FeedbackMapper">
    
    <resultMap type="Feedback" id="FeedbackResult">
        <result property="id"    column="id"    />
        <result property="userId"    column="user_id"    />
        <result property="contactInfo"    column="contact_info"    />
        <result property="status"    column="status"    />
        <result property="imageUrl"    column="image_url"    />
        <result property="content"    column="content"    />
        <result property="responseUserId"    column="response_user_id"    />
        <result property="response"    column="response"    />
        <result property="responseTime"    column="response_time"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
        <result property="delFlag"    column="del_flag"    />
    </resultMap>

    <sql id="selectFeedbackVo">
        select id, user_id, contact_info, status, image_url, content, response_user_id, response, response_time, create_by, create_time, update_by, update_time, del_flag from feedback
    </sql>

    <select id="selectFeedbackList" parameterType="Feedback" resultType="com.ruoyi.system.domain.vo.FeedbackListVO">
        select
            a.id,
            a.contact_info,
            a.status,
            a.image_url,
            a.content,
            a.response_time,
            a.create_by,
            a.create_time,
            b.user_name as responseUsername
        from feedback a left join sys_user b on a.response_user_id = b.user_id
        <where>
            <if test="status != null "> and a.status = #{status}</if>
            <if test="createBy != null  and createBy != ''"> and a.create_by = #{createBy}</if>
            and a.del_flag = 0
        </where>
    </select>
    

</mapper>